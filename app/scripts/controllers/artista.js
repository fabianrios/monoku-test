'use strict';

/**
 * @ngdoc function
 * @name lastFmApp.controller:ArtistaCtrl
 * @description
 * # ArtistaCtrl
 * Controller of the lastFmApp
 */
angular.module('lastFmApp')
  .controller('ArtistaCtrl', function ($scope, $routeParams) {
    
    console.log($routeParams.id);
    
  });
